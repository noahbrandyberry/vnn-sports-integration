<% content_for :title, "My Schools" %>
<% content_for :cta, (link_to 'New school', new_admin_school_path, class: "rounded-lg py-3 px-5 bg-primary text-white block font-medium") %>

<div class="w-full">
  <div id="schools" class="min-w-full">
    <ul role="list" class="divide-y divide-gray-400">
      <%= render @schools %>
    </ul>
  </div>
</div>
