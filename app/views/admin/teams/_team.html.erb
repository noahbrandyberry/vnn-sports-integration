<li id="<%= dom_id team %>" class="flex items-center gap-x-6 py-5">
  <a href="<%= admin_team_path team %>" class="flex-1">
    <p class="text-lg font-semibold leading-6 text-gray-900"><%= team.name %></p>
  </a>

  <div class="hidden shrink-0 sm:flex sm:flex-col sm:items-end">
    <%= link_to 'Edit this team', edit_admin_team_path(team), class: "text-primary" %>
  </div>
</li>

